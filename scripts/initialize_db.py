from scripts.database_utils import create_tables

if __name__ == "__main__":
    create_tables()

"""
This script creates tables in the database. Runs only once at the beginning. Or if suddenly the tables were deleted.
"""
